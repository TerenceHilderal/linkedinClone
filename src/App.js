import Header from './Header';

const App = () => {
	return (
		<div>
			<h1 className='app'>Let's build a linkedin CLone</h1>
			<Header />
		</div>

		// header
		// app body
		// left side
		// feed
		// widgets
	);
};

export default App;
